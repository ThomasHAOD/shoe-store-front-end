import React from "react";
import classes from "./SearchComponent.module.scss";

const searchComponent = props => (
  <div className={classes.SearchComponent}>Search Component</div>
);
export default searchComponent;
